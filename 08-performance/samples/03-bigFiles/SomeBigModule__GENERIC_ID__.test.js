

import assert from 'assert';


import { SomeBigModule__GENERIC_ID__ } from './SomeBigModule__GENERIC_ID__';


describe('SomeBigModule__GENERIC_ID__', () => {

    // __SPLIT_POINT__
    it('.method__GENERIC_METHOD_ID__()', () => {
        const instance = new SomeBigModule__GENERIC_ID__(3, 8);
        assert.strictEqual(
            instance.method__GENERIC_METHOD_ID__(),
            11
        );
    });

    // __SPLIT_POINT__

});
